import { NextResponse } from 'next/server'; import prisma from '@/lib/prisma'; import { getSiteId } from '@/lib/siteGuard'; import { apiSuccess } from '@/core/errors'; export async function GET(req) { try { const siteId = getSiteId(req); const { searchParams } = new URL(req.url); const zoneSlug = searchParams.get('zone'); if (!zoneSlug) { return NextResponse.json({ error: 'Zone slug parameter is required' }, { status: 400 }); } const ads = await prisma.ad.findMany({ where: { isActive: true, zone: { siteId, slug: zoneSlug }, OR: [ { startDate: null }, { startDate: { lte: new Date() } } ], AND: [ { OR: [ { endDate: null }, { endDate: { gte: new Date() } } ]} ] }, select: { id: true, name: true, type: true, code: true, imageUrl: true, targetUrl: true } }); return NextResponse.json(apiSuccess({ ads })); } catch (err) { console.error('Public serve ads error:', err); return NextResponse.json({ error: 'Internal Server Error', message: err.message }, { status: 500 }); } }
